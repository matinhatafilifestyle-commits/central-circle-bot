# central-circle-bot

Simple Telegram management bot for the "Daire Markazi" group.

Environment variables:
- TG_BOT_TOKEN (required)
- ADMIN_ID (optional, numeric)
- DATABASE_URL (optional, e.g. postgres url)

Run:
pip install -r requirements.txt
python bot.py
